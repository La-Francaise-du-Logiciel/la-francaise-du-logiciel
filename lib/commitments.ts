/**
 * The figures behind the commitments, paired with the catalogue's stats by
 * position.
 *
 * These are promises we control, not measurements we cannot yet back up.
 * The unit that goes after each one is typography, so it stays in the
 * catalogue. They live here rather than in the component because the
 * markdown rendering of the home page states the same numbers, and a
 * promise that reads differently in two places is worse than no promise.
 */
export const COMMITMENT_FIGURES: readonly number[] = [1, 24, 100, 2]
